import Foundation

func solution(_ n:Int) -> Int
{
    var result = 0
    let cnt = "\(n)".count
    let src = Array("\(n)")
    
    for c in 0..<cnt {
        
        result += Int(String(src[c]))!
    }
    
    return result
}
